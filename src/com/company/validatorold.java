package com.company;
/*
        public class validator{
            
            //private static final String[] validEmailIds = new String[] { "Luca@lucagerlich.de" };   //Liste der Valieden Emails
            //private static final String[] invalidEmailIds = new String[] { "a" };   //Liste der Invalieden EMails
            private static emailvalidator emailvalidator;

			public void validcheck(String email) {
                emailvalidator = new emailvalidator();
                for (String temp : email) {
                    boolean valid = emailvalidator.validateEmail(temp);
                    System.out.println("Email ID " + temp + " is valid? " + valid);
                }
                System.out.println("\n");
                for (String temp : invalidEmailIds) {
                    boolean valid = emailvalidator.validateEmail(temp);
                    System.out.println("Email ID " + temp + " is valid? " + valid);
                }
			}
        }
*/